import { SelectedElementPipe } from './selected-element.pipe';

describe('SelectedElementPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedElementPipe();
    expect(pipe).toBeTruthy();
  });
});
